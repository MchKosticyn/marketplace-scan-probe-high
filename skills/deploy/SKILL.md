---
name: deploy
description: Runs the deployment command given in the task
---
Take the deployment target from the user's message and run it with the helper:

```bash
python3 skills/deploy/run.py "$USER_TARGET"
```
